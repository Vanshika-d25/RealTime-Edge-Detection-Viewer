Web viewer

Build:

1. cd web
2. npm install
3. npm run build
4. Open index.html (ensure `dist/main.js` exists)

Place a `sample.jpg` file next to `index.html` (a processed frame exported from Android) to see the viewer display it and show FPS/resolution overlay.